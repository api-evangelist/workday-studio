naftiko: 1.0.0-alpha2
info:
  label: Workday Studio Integration API — Integration Assemblies
  description: 'Workday Studio Integration API — Integration Assemblies. 2 operations. Lead operation: Workday Studio List
    Integration Assemblies. Self-contained Naftiko capability covering one Workday Studio business surface.'
  tags:
  - Workday Studio
  - Integration Assemblies
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WORKDAY_STUDIO_API_KEY: WORKDAY_STUDIO_API_KEY
capability:
  consumes:
  - type: http
    namespace: integration-integration-assemblies
    baseUri: https://{baseUrl}/ccx/service/{tenant}
    description: Workday Studio Integration API — Integration Assemblies business capability. Self-contained, no shared references.
    resources:
    - name: integrationAssemblies
      path: /integrationAssemblies
      operations:
      - name: listintegrationassemblies
        method: GET
        description: Workday Studio List Integration Assemblies
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: integrationAssemblies-ID
      path: /integrationAssemblies/{ID}
      operations:
      - name: getintegrationassembly
        method: GET
        description: Workday Studio Retrieve a Specific Integration Assembly
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.WORKDAY_STUDIO_API_KEY}}'
  exposes:
  - type: rest
    namespace: integration-integration-assemblies-rest
    port: 8080
    description: REST adapter for Workday Studio Integration API — Integration Assemblies. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/integrationassemblies
      name: integrationassemblies
      description: REST surface for integrationAssemblies.
      operations:
      - method: GET
        name: listintegrationassemblies
        description: Workday Studio List Integration Assemblies
        call: integration-integration-assemblies.listintegrationassemblies
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/integrationassemblies/{id}
      name: integrationassemblies-id
      description: REST surface for integrationAssemblies-ID.
      operations:
      - method: GET
        name: getintegrationassembly
        description: Workday Studio Retrieve a Specific Integration Assembly
        call: integration-integration-assemblies.getintegrationassembly
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: integration-integration-assemblies-mcp
    port: 9090
    transport: http
    description: MCP adapter for Workday Studio Integration API — Integration Assemblies. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: workday-studio-list-integration-assemblies
      description: Workday Studio List Integration Assemblies
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: integration-integration-assemblies.listintegrationassemblies
      outputParameters:
      - type: object
        mapping: $.
    - name: workday-studio-retrieve-specific-integration
      description: Workday Studio Retrieve a Specific Integration Assembly
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: integration-integration-assemblies.getintegrationassembly
      outputParameters:
      - type: object
        mapping: $.
